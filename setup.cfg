[metadata]
name = afaligner
version = 0.2.0
description = Audio–subtitle aligner with custom DTW core

[options]
packages = find:
install_requires =
    cffi>=1.14
zip_safe = false

[options.packages.find]
where = src
